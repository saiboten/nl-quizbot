const tools = require("../functions.js");

module.exports = {
  admin: true,
  name: "end",
  description: "Avslutt quiz",
  execute(message) {
    if(tools.isAllowedToIssuecommand(message)) {
      // TODO
    }
  },
};
